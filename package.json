{
  "name": "trnsprncy",
  "version": "0.0.1",
  "private": true,
  "engines": {

  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "cli": "npm run -w packages/cli cli",
    "init": "npm run -w packages/cli init"
  }
}
